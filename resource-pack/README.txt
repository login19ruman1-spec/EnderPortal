EnderPortal starter resource pack.

The included purple_rift.png is an EnderPortal visual asset. A fully permanent purple sky requires a compatible client shader/rendering override; the plugin supports sending this pack after the explosion when a hosted HTTPS URL is configured.
